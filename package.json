{
  "name": "supabase-upload",
  "version": "1.0.0",
  "description": "upload files to supabase bucket",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/action.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Torwent",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@actions/core": "^1.7.0",
    "@supabase/supabase-js": "^1.35.2",
    "@vercel/ncc": "^0.33.4"
  }
}
